# Webex Bot Configuration
# Copy this file to .env and fill in your credentials

# =============================================================================
# Bot Token (Required)
# =============================================================================
# Your Webex bot's access token
# Get this from https://developer.webex.com/my-apps when you create a bot
BOT_TOKEN=your_bot_token_here

# =============================================================================
# OAuth Integration Credentials (for admin token via OAuth flow)
# =============================================================================
# These are used by oauth.py to get admin tokens through the OAuth flow
# Get these from https://developer.webex.com/my-apps when you create an Integration

# OAuth Client ID
OAUTH_CLIENT_ID=your_client_id_here

# OAuth Client Secret
OAUTH_CLIENT_SECRET=your_client_secret_here

# OAuth Redirect URI (must match what's configured in your Integration)
# Default: http://127.0.0.1:9999/auth
OAUTH_REDIRECT_URI=http://127.0.0.1:9999/auth
